# Project Updates

## 2026-06-15

- Scaffolded a standalone ED web platform inside `data/ed-web-platform`.
- Reused the existing `energy_benchmark.py` comparison engine through a package-local wrapper.
- Added a FastAPI backend, browser UI, local storage for runs, and GitHub Actions CI.
- Added fixture-based API tests that exercise both folder uploads and zip uploads.
